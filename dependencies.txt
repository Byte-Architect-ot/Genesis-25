# Update package list
sudo apt update

# Install build tools
sudo apt install -y build-essential cmake git

# Install SFML library
sudo apt install -y libsfml-dev

# Install SQLite3
sudo apt install -y libsqlite3-dev

# Install OpenGL (needed for ImGui)
sudo apt install -y libgl1-mesa-dev